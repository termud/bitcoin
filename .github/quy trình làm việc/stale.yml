# This workflow warns and then closes issues and PRs that have had no activity for a specified amount of time.
#
# You can adjust the behavior by modifying this file.
# For more information, see:
# https://github.com/actions/stale
tên : Đánh dấu các vấn đề cũ và yêu cầu kéo
[![Mark stale issues and pull requests](https://github.com/termud/bitcoin/actions/workflows/stale.yml/badge.svg?branch=AIphanbade%E2%81%B9%C2%B3&event=branch_protection_rule)](https://github.com/termud/bitcoin/actions/workflows/stale.yml)
trên :
  schedule:
  - cron: '42 12 * * *'

việc làm :
  cũ :

    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
    - uses: actions/stale@v3
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        stale-issue-message: 'Stale issue message'
        stale-pr-message: 'Stale pull request message'
        stale-issue-label: 'no-issue-activity'
        stale-pr-label: 'no-pr-activity'
